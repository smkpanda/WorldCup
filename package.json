{
  "name": "world-cup-predictor",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "test": "npm --prefix web run test && python -m unittest discover -s predictor/tests -t predictor -v"
  }
}
